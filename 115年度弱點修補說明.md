# 115 年度網站弱點修補說明（衛保組）

## 本版已處理項目

1. 移除程式碼中的 Portal Basic Auth、Flask Secret Key 與 JWT Secret Key，改由主機環境變數提供。
2. OAuth 登入加入隨機 `state`、安全 Cookie 與 callback 驗證，降低登入偽造與 CSRF 風險。
3. 登入與登出改由後端固定路由處理，不再由前端帶入可控制的轉址網址。
4. 移除 404 頁面回顯網址參數的做法，動態內容 ID 只接受數字，降低開放轉址與輸入注入風險。
5. 使用者清單、權限修改與刪除 API 增加管理者身分檢查。
6. CORS 改為允許清單，只開放設定的正式站台來源。
7. 前後端加入 CSP、HSTS、禁止 MIME sniffing、禁止 iframe 嵌入、Referrer Policy 等安全標頭。
8. 登入、登出、管理區及認證 API 加入 `no-store`，避免敏感頁面被瀏覽器或代理快取。
9. 更新 Next.js、Axios、lodash、js-cookie 等相依套件，並完成 Next.js 15 非同步路由參數相容調整。
10. 移除版本庫內所有 `__pycache__` 與 `.pyc`，並加入忽略規則，之後不應再提交這些檔案。
11. 新增 OAuth state、安全標頭、快取與 CORS 自動測試。

## 部署前一定要做

1. 將 `.env.example` 複製為 `.env`，填入新的密鑰與 Portal OAuth 資料；`.env` 不可提交到 GitHub。
2. 因舊的 Portal 憑證曾存在程式碼及 Git 歷史，請向 Portal 管理單位撤銷並重發，不能只把舊值移出程式碼。
3. 產生至少 32 bytes 的隨機 `FLASK_SECRET_KEY` 與 `JWT_SECRET_KEY`，兩者不要相同。
4. 主機的 Nginx、作業系統、SSH 與 TLS 設定屬於主機層，仍需由主機管理者更新與限制；本 ZIP 只包含網站程式修補。
5. 在正式主機先備份資料庫與 `backend/statics` 上傳檔，再部署並執行複測。

## 驗證結果

- 前端：`npm run build` 通過（Next.js 15.5.23）。
- 後端：`pytest -q test/test_security.py`，4 項測試全部通過。
- 建置時仍有原專案既有的 React Hook／Edge Runtime 警告，但未阻擋建置；後續維護可再逐步整理。
- 依 2026-08-19 的套件稽核，仍有必須升級到 Next.js 16 與編輯器套件改版才能完全排除的相依套件警示。這些是跨主要版本變更，不應未經完整功能測試直接上正式站。

## 建議部署順序

```text
本機解壓縮與測試 → 備份正式站資料 → 設定主機 .env → 推送 GitHub
→ 在主機拉取程式碼 → 重新建置容器 → 測試登入/後台/上傳 → 申請弱掃複測
```
